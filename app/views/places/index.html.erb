<div class="container-fluid">
  <div class="row">
    <div class="col-md-2">
        <h2>All Places</h2>
    </div>
    <div class="col-md-10">
      <h2>
        <% if current_user %>
          <%= link_to 'Create a New Place', new_place_path(), class: "btn btn-primary" %>
        <% end %>
      </h2>
    </div>
  </div>

  <% @places.each do |i| %>
    <div class="row" style="padding:0px 0px 20px">
      <div class="col-md-12">
        <div class="row">
          <div class="col-md-2">
            <i class="fa fa-building" style="font-size:50px;"></i>
          </div>
          <div class="col-md-2">
          <h5>
            <%= link_to i.name, place_path(i) %>
          </h5>
          </div>
          <div class="col-md-8">
            <p>
              description
            </p>
          </div>
        </div>
      </div>
    </div>
    <% end %>
</div>